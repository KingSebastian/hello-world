# yaml-language-server: $schema=https://cdn.raw-labs.com/endpoint-schemaV0.9.json
raw: 0.9
endpoint: GET
metadata:
  title: All fun facts
  description: List of fun facts.
  tags:
    - facts
code: rql
declaration: fun_fact
codeFile: hello_world.rql
format: json
security:
  public: true
computeClass: normal
enabled: true
tests:
  - name: fun_fact
    description: Run the 'All facts' endpoint.
